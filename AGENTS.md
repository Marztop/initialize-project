# AGENTS.md

## 项目定位

- 本仓库是可直接安装的 Codex Skill `initialize-project`。
- Skill 只在用户明确要求初始化新项目或增量初始化已有项目时工作，不因初始化文件缺失而自动介入普通开发任务。
- 默认文档语言使用中文；Skill 指令和公开元数据使用清晰英文，以便跨语言触发和复用。

## 设计与规则边界

- `.init_repo/design/design_doc.md` 是项目目标、技术选型、架构、运行环境和阶段路线图的唯一事实来源。
- 本文件只规定维护本仓库时 Agent 必须遵守的工作方式，不重复技术选择或阶段计划。
- 修改 Skill 的产品边界、交互流程、资料结构或长期约定前，先阅读设计文档；需要改变已确认决策时先向用户说明影响。

## Skill 结构

- `SKILL.md`：核心触发条件、守则和执行流程，保持简洁并使用祈使语气。
- `agents/openai.yaml`：用户界面元数据，必须与 `SKILL.md` 一致。
- `references/`：只存放执行时按需读取的访谈、文档和版本控制规范。
- `.init_repo/`：本 Skill 仓库自身的设计、调研和实施记录，不属于安装后生成到目标项目的固定模板。
- `CONTEXT.md`：本仓库的领域词汇表，不写实现细节。

## 编辑约束

- 保持 Skill 自包含，不依赖 `grill-with-docs`、`domain-modeling` 或其他用户本地 Skill。
- 不加入固定技术栈、前端框架或运行环境偏好；这些选择必须由目标项目事实和用户决定。
- 不自动覆盖已有 `AGENTS.md`、设计文档或项目资产。
- 不把测试、工具、依赖清单、源代码或用户文档放入目标项目的 `.init_repo/`。
- 不新增 README、安装指南、变更日志或无直接执行价值的辅助文档。

## 验证

- 修改 Skill 后运行 `skill-creator/scripts/quick_validate.py`。
- 重新读取变更文件，运行 `git diff --check`，确认没有 TODO、占位文本或断裂链接。
- 行为发生实质变化时，使用隔离的空项目和已有项目场景进行前向测试，避免测试 Agent 看到预期答案。
