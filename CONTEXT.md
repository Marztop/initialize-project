# 项目初始化 Skill

本项目提供可复用的项目初始化能力，让 Agent 根据目标项目的实际情况建立开发规则、设计记录和验证闭环。

## 领域语言

**项目初始化**：
在正式开发前，根据目标项目的现状和用户确认的边界，建立或调整项目规则、设计记录、运行环境约定与配套目录。
_避免_：模板复制、脚手架复制

**增量初始化**：
在保留已有项目规则和资料的前提下，识别缺口并补充初始化内容；任何既有规则变更都需要用户确认。
_避免_：重新初始化、覆盖初始化

**项目级工作约定**：
持续约束多个后续任务和 Agent 行为的全项目规则，不包含单项功能需求、阶段计划或一次性实现选择。
_避免_：抽象约定、项目需求、阶段任务

**初始化资料**：
由 Agent 在项目初始化和后续工作中维护，用于说明项目设计、事实依据与实施历史的内部资料。
_避免_：项目文档、项目资产

**项目资产**：
由目标项目直接构建、运行、测试、发布或交付的代码、配置、依赖清单与文档。
_避免_：初始化资料、Agent 资料

**共享初始化资料**：
纳入目标项目版本控制、随仓库提供给所有协作者的初始化资料。
_避免_：本机私有初始化资料

**本机私有初始化资料**：
只服务当前本地工作副本、不进入目标项目版本控制的初始化资料。
_避免_：共享初始化资料、项目资产

**交付阶段**：
具有明确目标、范围、交付物和验收标准，可以独立确认完成的项目增量。
_避免_：时间周期、任务清单、Sprint
